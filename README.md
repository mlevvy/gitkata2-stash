To prepare to this workshop execute following commands:
- unzip git-stash.zip
- cd git-stash
- git init
- git add .
- git commit -m "Initial commit"

Do not checkout branch solution, before kata, because there are solutions ;p
